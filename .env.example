# COMPOUND environment variables.
# Copy this file to `.env.local` and fill in your values. `.env.local` is gitignored.
#
# Phase 2 — Supabase (client-side; safe to expose, protected by Row Level Security):
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Phase 4 — Anthropic API key is SERVER-ONLY and must NOT live here / in the client.
# It goes in Vercel → Project → Settings → Environment Variables as ANTHROPIC_API_KEY.
